#coding=utf-8
'''
author: chenfei
update: 2018-4-13 11:58:20

'''
import json
from datetime import datetime
from datetime import date

class CustomizeDateTime(json.JSONEncoder):
    '''
    自定义json处理器
    '''
    def default(self, field):
        if isinstance(field, datetime):
            return field.strftime('%Y-%m-%d %H:%M:%S')
        elif isinstance(field, date):
            return field.strftime('%Y-%m-%d')
        else:
            return json.JSONEncoder.default(self, field)



def get_json(obj):
    return json.loads(
        json.dumps(obj, cls=CustomizeDateTime)
        )

#print json.dumps(datetime.now(), cls=CustomizeDateTime)
#
